spring:
  application:
    name: MyFirstProject
  datasource:
    url: jdbc:mysql://localhost:3306/college
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true